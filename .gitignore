# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Secrets & Credentials
.env
.env.local
*.pem
*.key
*-service-account.json
*_credentials.csv
*_accessKeys.csv
*accessKeys*.csv
AccessKey-*.csv
accesskey-*.csv
credentials/
~/.skyrchitect/
.skyrchitect/
infra/alibaba/backend/.deploy/

# Build artifacts
__pycache__/
*.pyc
node_modules/
dist/
coverage/

# skyrchitect: monorepo sync marker (no ignore-behavior change)

# Python
venv/
.venv/
__pycache__/
*.pyc

# Credentials — never commit
# Broad patterns: a suffixed name like eimis-bot-hacks.json must not slip past
# a narrow `*-bot.json` rule.
cerebras-bot.json
*bot*.json
*accessKeys*.csv
*credentials*.csv
*-key.json
*service*account*.json
gcp-credentials*.json
*.pem
*.p12

# NVIDIA tester — browser session (Slack login cookies!) and run artifacts
nvidia/tester/.slack-profile/
nvidia/tester/runs/
# stray debug logs from tester/agent runs
*-stderr.log
*-stderr2.log
tester-stderr.log
tests/pytest.log

# pytest logs (copied around by sync)
**/pytest.log
