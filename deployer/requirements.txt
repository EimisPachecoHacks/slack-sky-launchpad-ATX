cryptography>=41.0.0

# Self-improvement loop (all imported lazily; the loop degrades gracefully if missing)
# The repair agent calls Gemma 4 through backend.llm_client (httpx), not an SDK.
google-cloud-logging>=3.0.0  # Cloud Logging enrichment on deployment failure (GCP)
requests>=2.31.0             # REST fallback transport for Cloud Logging
