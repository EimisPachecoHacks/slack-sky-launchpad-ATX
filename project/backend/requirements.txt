## Skyrchitect AI Backend Requirements

# Core Framework
fastapi>=0.115.6  # Updated to support starlette>=0.46.2
uvicorn[standard]>=0.34.2
pydantic>=2.12.0
pydantic-settings>=2.6.1

# AI — Qwen Cloud (Alibaba Model Studio) speaks the OpenAI wire format, so the
# only client we need is an HTTP client. See backend/llm_client.py.
httpx>=0.27.0
requests>=2.31.0  # GitLab + Duo clients

# Utilities
python-dotenv==1.0.1
python-multipart==0.0.12

# Security
PyJWT>=2.10.1,<3.0.0  # JWT authentication
python-magic==0.4.27  # MIME type detection for file validation
cryptography>=41.0.0  # deployer/credential_manager encrypts stored cloud creds

# Image Processing & Analysis
Pillow==11.3.0
pdf2image==1.16.3
PyPDF2==3.0.1

# Testing Dependencies
pytest>=8.0.0  # Core testing framework
pytest-asyncio>=0.23.0  # Async test support
pytest-cov>=4.1.0  # Code coverage reporting
pytest-mock>=3.12.0  # Enhanced mocking capabilities

# Optional for production
# gunicorn==21.2.0  # For production deployment
# mangum==0.17.0    # For AWS Lambda deployment
boto3>=1.34.0  # AWS S3 storage backend (optional)
logfire[fastapi,httpx]>=0.50.0  # observability/tracing (Self-Improvement Stack)
pymongo>=4.6  # MongoDB Atlas (skills/apps/tests store)
certifi>=2024.2.2  # TLS trust store for Atlas on macOS/python.org builds
